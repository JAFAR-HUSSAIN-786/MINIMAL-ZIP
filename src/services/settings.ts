import AsyncStorage from '@react-native-async-storage/async-storage';const KEY='JB_SETTINGS';export async function getSettings(){const raw=await AsyncStorage.getItem(KEY);if(!raw){const def={startingBalancePKR:100000,compoundingMode:'FULL',riskPercent:1};await AsyncStorage.setItem(KEY,JSON.stringify(def));return def;}return JSON.parse(raw);}export async function saveSettings(s){await AsyncStorage.setItem(KEY,JSON.stringify(s));return s;}

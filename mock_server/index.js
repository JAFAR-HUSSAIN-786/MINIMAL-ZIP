const express=require('express');const app=express();app.use(require('body-parser').json());app.post('/api/trades/sync',(req,res)=>{ const q = Array.isArray(req.body)?req.body:[]; const conf = q.map((it,i)=>({localId:it.localId, tradeId:'srv_'+Date.now()+'_'+i, status:'EXECUTED', profitPercent:5})); setTimeout(()=>res.json(conf),300); }); app.listen(4000,()=>console.log('mock server on 4000'));